"""
if _name_ == __main__: (this script can be imported OR run standalone)

Functions and classes in this module can be reused without the main block of code executing

Good coding practice (code is modular, helps readability, leaves no global variables, avoids unintended execution)

E.g. library = Import library for functionality.  When running library directly, display a help page.

"""

def main():
    # Main code to type i.e. program to be run and executed under this file

if __name__ == "__main__":
    main() 