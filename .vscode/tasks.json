{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PostgreSQL: Run SQL file (psql)",
            "type": "shell",
            "command": "psql",
            "args": [
                "-f",
                "${file}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}