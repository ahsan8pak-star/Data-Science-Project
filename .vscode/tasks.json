{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "PostgreSQL: Run SQL file (psql)",
            "type": "shell",
            "command": "psql",
            "args": [
                "-f",
                "${file}"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        },
        {
            "label": "Sync: Update Requirements (requirements_sync.py)",
            "type": "shell",
            "command": ".venv\\Scripts\\python.exe requirements_sync.py",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}